function [dl,dtheta] = GetDelta(w_r,w_l)
%GetDelta: Calcula deltas obviamente
% Calcula el delta de velocidad lineal y angular del robot a partir de la velocidad
% angular de ambas ruedas.
outputArg1 = inputArg1;
outputArg2 = inputArg2;
end